using System;

class Program
{
    static void Main()
    {
        int result = Mul(4, 5); // Example usage
        Console.WriteLine("The result is: " + result);
    }

    static int Mul(int a, int b)
    {
        return a * b;
    }
}
